<%= form_for(patrol) do |f| %>
  <% if patrol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patrol.errors.count, "error") %> prohibited this patrol from being saved:</h2>

      <ul>
      <% patrol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
