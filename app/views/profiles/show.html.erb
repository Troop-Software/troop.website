<h1 class="text-center">Welcome to <%= @user.username %>'s page</h1>

<div class="row">
  <div class="text-center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h4 class="text-center"><%= @user.username %>'s articles</h4>
<div class="tw_pagination"><%= will_paginate @user_articles %></div>
<%= render 'articles/articles', obj: @user_articles %>
<div class="tw_pagination"><%= will_paginate @user_articles %></div>
